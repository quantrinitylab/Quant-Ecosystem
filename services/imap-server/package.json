{
  "name": "@quant/imap-server",
  "version": "1.0.0",
  "private": true,
  "description": "RFC 3501 Outbound IMAP4rev1 Daemon on Port 993/143 serving QuantMail mailboxes to native clients",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@quant/auth": "workspace:*",
    "@quant/database": "workspace:*",
    "argon2": "^0.41.1",
    "ioredis": "^5.6.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "~5.5.0",
    "vitest": "^4.0.0"
  }
}
