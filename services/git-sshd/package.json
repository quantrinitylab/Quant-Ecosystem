{
  "name": "@quant/git-sshd",
  "version": "1.0.0",
  "private": true,
  "description": "Git over SSH Daemon on Port 22/2222 authenticating user public keys for git push and clone",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsx watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@quant/database": "workspace:*",
    "ssh2": "^1.15.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ssh2": "^1.15.1",
    "typescript": "~5.5.0",
    "vitest": "^4.0.0"
  }
}
