{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["prisma/schema.prisma", "src/**"],
      "outputs": ["../../node_modules/.prisma/client/**"]
    }
  }
}
