{
  "name": "@quant/federation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "dependencies": {
    "matrix-bot-sdk": "^0.8.0",
    "zod": "^3.24.2",
    "pino": "^9.6.0",
    "@quant/common": "workspace:*",
    "@quant/queue": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "vitest": "^3",
    "typescript": "~5.9.3"
  }
}
